{{#website-docs 
  anchor="assets"
  sections=(append
    (hash 
      name="Getting Started" 
      links=(append
        (hash path="index" text="Quick Start")))
    (hash
      name="Components"
      links=(append
        (hash path="expositions" text="Expositions")
        (hash path="textbase-products" text="Text-base Product Card")
        (hash path="minihero" text="Mini Hero")
        (hash path="grid-cards" text="Grid Cards"))))}}
  {{outlet}}
{{/website-docs}}